describe("example user story alignment", () => {
  // Given a requirement with a simple rule
  // When we evaluate the logic
  // Then it should behave as documented
  it("keeps the placeholder test green", () => {
    expect(true).toBe(true);
  });
});
